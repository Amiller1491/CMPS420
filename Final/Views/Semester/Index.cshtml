@model IEnumerable<Final.Models.SemesterModel>

@{
    ViewBag.Title = "Index";
}

<h2>Semesters</h2>
@*<button id="addButton" class="btn btn-primary"><i class="icon-plus"> </i> </button>*@

@if(User.IsInRole("Head")){
<p>
   @* @Html.ActionLink("Add User", "Create",null, new { @class="btn-primary"})*@
    <a class="btn btn-primary" href="~/Semester/Create" ><i class="icon-plus"></i> Add Semester</a>
</p>
}

<table class="table tbody table-striped tbody table-hover tbody tr:hover table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SemYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SemYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SemID }, new { @class="btn btn-mini"}) |
            @Html.ActionLink("Details", "Details", new { id=item.SemID }, new { @class="btn btn-mini"}) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SemID }, new { @class="btn btn-mini btn-danger"})
        </td>
    </tr>
}

</table>
