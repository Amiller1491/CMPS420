@model Final.Models.SemesterModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SemesterModel</legend>

        @Html.HiddenFor(model => model.SemID)

        <div class="editor-label">
            @Html.LabelFor(model => model.SemName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SemName)
            @Html.ValidationMessageFor(model => model.SemName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SemYear)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SemYear)
            @Html.ValidationMessageFor(model => model.SemYear)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
