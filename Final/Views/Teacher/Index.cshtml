@model IEnumerable<Final.Models.TeacherModel>

<h2>Teachers</h2>
@*<button id="addButton" class="btn btn-primary"><i class="icon-plus"> </i> </button>*@

@if(User.IsInRole("Head")){
<p>
   @* @Html.ActionLink("Add User", "Create",null, new { @class="btn-primary"})*@
    <a class="btn btn-primary" href="~/Teacher/Create" ><i class="icon-plus"></i> Add Teacher</a>
</p>
}

<table class="table tbody table-striped tbody table-hover tbody tr:hover table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instructor_FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instructor_LName)
        </th>
        <th>Actions</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor_FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor_LName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Instructor_ID }, new { @class="btn btn-mini"}) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Instructor_ID }, new { @class="btn btn-mini btn-danger"})
        </td>
    </tr>
}

</table>
