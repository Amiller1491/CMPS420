@model Final.Models.TeacherModel

@{
    ViewBag.Title = "Edit";
}

</br>
</br>
</br>

<div align="center" class="well offset3 span5">

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
     
<fieldset>
    <legend><h2>Edit Teacher</h2></legend>

        @Html.HiddenFor(model => model.Instructor_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.Instructor_FName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Instructor_FName)
            @Html.ValidationMessageFor(model => model.Instructor_FName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Instructor_LName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Instructor_LName)
            @Html.ValidationMessageFor(model => model.Instructor_LName)
        </div>

        <p>
            <input class="btn btn-small btn-primary" type="submit" value="Save" /> |
             @Html.ActionLink("Back", "Index", "", new { @class="btn btn-mini"})
        </p>
     
    </fieldset>
}
    </div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
