@model Final.Models.TeacherModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TeacherModel</legend>

        @Html.HiddenFor(model => model.T_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.T_FName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.T_FName)
            @Html.ValidationMessageFor(model => model.T_FName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.T_LName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.T_LName)
            @Html.ValidationMessageFor(model => model.T_LName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
