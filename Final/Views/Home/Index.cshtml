@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
</br>
</br>



<div class="hero-unit" style="text-align:center">

    @if(User.Identity.IsAuthenticated){

            /*
             if (User.IsInRole("Head")){
                 
                <h1> Hello, @HttpContext.Current.User.Identity.Name</h1>     
                <h2> You are the Department Head</h2>
            }
            if (User.IsInRole("Coordinator")){
                <h1> Hello, @HttpContext.Current.User.Identity.Name</h1>     
                <h2> You are a Department Coordinator</h2>
            }
              */
            
            if (User.IsInRole("Head")){
                 
                <h1> Hello, @HttpContext.Current.User.Identity.Name</h1>     
                <!--<h2> You are the Department Head</h2>-->
            }
            if (User.IsInRole("Coordinator")){
                <h1> Hello, @HttpContext.Current.User.Identity.Name</h1>     
                <!--<h2> You are a Coordinator</h2>-->
            }
            if (User.IsInRole("Assistant")){
                <h1> Hello, @HttpContext.Current.User.Identity.Name</h1>     
                <!--<h2> You are an Assistant</h2>-->
            }
             
    
            <!--
                <br />

              
              <p>
                <a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-primary btn-large btn-success">
                  Log Out
                </a>
              </p>
                -->
        
    }
    
    @if(!User.Identity.IsAuthenticated){
    

    <h1>Welcome! Ready to Schedule?</h1>
        <br />

    }

</div>
<div class="row-fluid">
                <div class="span4">
                    <h2>Notifications</h2>
                    <p>This is where your notifications can go.</p>
                    <p><a class="btn" href="#">View Details >></a></p>
                </div>
                <div class="span4">
                    <h2>Alerts</h2>
                    <p>This is where your alerts can go.</p>
                    <p><a class="btn" href="#">View Details >></a></p>
                </div>
</div>       







