@model IEnumerable<Final.Models.CourseModel>

@{
    ViewBag.Title = "Index";
}

<h2>Courses</h2>
@*<button id="addButton" class="btn btn-primary"><i class="icon-plus"> </i> </button>*@

@if(User.IsInRole("Head")){
<p>
   @* @Html.ActionLink("Add User", "Create",null, new { @class="btn-primary"})*@
    <a class="btn btn-primary" href="~/Course/Create" ><i class="icon-plus"></i> Add Course</a>
</p>
}

<table class="table tbody table-striped tbody table-hover tbody tr:hover table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.C_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.C_Section)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.C_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.C_Section)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.C_Num }, new { @class="btn btn-mini"}) |
            @Html.ActionLink("Delete", "Delete", new { id=item.C_Num }, new { @class="btn btn-mini btn-danger"})
        </td>
    </tr>
}

</table>
