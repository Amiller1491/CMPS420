@model Final.Models.CourseModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CourseModel</legend>

        @Html.HiddenFor(model => model.C_Num)

        <div class="editor-label">
            @Html.LabelFor(model => model.C_Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.C_Name)
            @Html.ValidationMessageFor(model => model.C_Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.C_Section)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.C_Section)
            @Html.ValidationMessageFor(model => model.C_Section)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
